<Page
    x:Class="VBAudioRouter.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VBAudioRouter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:media="using:Microsoft.UI.Xaml.Media"
    mc:Ignorable="d">

    <Page.Resources>
        <media:AcrylicBrush x:Key="AcrylicBackgroundBrush" BackgroundSource="Backdrop"
            TintOpacity="0.8" TintColor="#232323" FallbackColor="#232323" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Background="{StaticResource AcrylicBackgroundBrush}" CornerRadius="10" Grid.Column="0" />
        <Border Background="{StaticResource AcrylicBackgroundBrush}" CornerRadius="10" Grid.Column="2" />
    </Grid>
</Page>
