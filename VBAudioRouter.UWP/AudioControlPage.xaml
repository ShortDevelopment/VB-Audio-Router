<Page
    x:Class="VBAudioRouter.AudioControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VBAudioRouter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Padding="10">
            <ComboBox HorizontalAlignment="Stretch" x:Name="OutputDevicesComboBox" PlaceholderText="Select Device" SelectionChanged="OutputDevicesComboBox_SelectionChanged" />
        </Grid>
        <Frame x:Name="ContentFrame" Grid.Row="1" />
    </Grid>
</Page>
